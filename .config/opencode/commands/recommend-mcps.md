---
description: Recommend MCP servers for the current project using local semantic search
subtask: true
---

Use the `recommend_mcps` tool to analyze this project and suggest relevant MCP
servers from the catalog. Present **only** the servers returned by the tool —
do not suggest, mention, or infer any MCP servers from your own knowledge.
For each result, offer to install it with `/install-mcp <name>`.
