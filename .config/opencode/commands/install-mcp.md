---
description: Install an MCP server from the catalog into the current project's .opencode/opencode.jsonc
subtask: true
---

Load the `install-mcp` skill and follow its instructions to install the MCP
server named `$ARGUMENTS` into the current project.
